FROM ghcr.io/costaalso2/webdav:main

# Forzamos al servidor a usar el puerto que Render quiere
ENV PORT=10000
EXPOSE 10000

# Comando para arrancar asegurando el puerto
CMD ["/usr/sbin/apachectl", "-D", "FOREGROUND", "-p", "10000"]
