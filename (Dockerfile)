FROM ghcr.io/costaalso2/webdav:main

# Forzamos a que acepte conexiones de cualquier lado sin error 405
RUN echo "DavLockDB /var/lib/dav/DavLock" >> /etc/apache2/apache2.conf
RUN mkdir -p /var/lib/dav/data && chown -R www-data:www-data /var/lib/dav/data

# Abrimos la puerta total
EXPOSE 80
